---

---

<div class="center">
  <slot />
</div>

<style>
  .center {
    box-sizing: content-box;
    margin-inline: auto;
    width: calc(100% - (var(--space-xl) * 2));
    max-inline-size: 760px;
    padding-inline: var(--space-xl);
  }
</style>

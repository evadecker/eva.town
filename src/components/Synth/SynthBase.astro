---

---

<Fragment>
  <g id="synth-base">
    <rect class="synth-base" x="0" y="0" width="270" height="156" rx="12"
    ></rect>
  </g>
</Fragment>

<style>
  .synth-base {
    fill: var(--synth-base);
    stroke: var(--synth-stroke);
    stroke-width: 3px;
    vector-effect: non-scaling-stroke;
    filter: drop-shadow(0 3px 0 var(--synth-stroke));
  }
</style>
